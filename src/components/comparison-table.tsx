export function ComparisonTable({ rows }: { rows: { useCase: string; choice: string; reason: string }[] }) { return <div className="comparison-table overflow-x-auto border border-slate-200" tabIndex={0} aria-label="Tool comparison table"><table className="w-full min-w-[640px] text-left text-sm"><caption className="sr-only">Tools compared by ecommerce use case</caption><thead className="bg-slate-950 text-white"><tr><th scope="col">Use case</th><th scope="col">Start with</th><th scope="col">Why</th></tr></thead><tbody>{rows.map((row) => <tr key={row.useCase} className="border-t border-slate-200"><th scope="row" className="font-bold">{row.useCase}</th><td>{row.choice}</td><td className="text-slate-600">{row.reason}</td></tr>)}</tbody></table></div>; }
